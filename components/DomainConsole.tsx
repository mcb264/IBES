"use client";
import { useEffect,useState } from "react"; import Link from "next/link";
import {Domain,DomainState,loadDomainState,saveDomainState,loadModeRouge,saveModeRouge,emptyReviewDraft,syncWeeklyCompletedTasks,BriefingData,TaskItem} from "@/lib/storage";
import DomainIcon from "@/components/DomainIcon"; import ModeRougeSwitch from "@/components/ModeRougeSwitch"; import BriefingPanel from "@/components/BriefingPanel"; import DumpPanel from "@/components/DumpPanel"; import ReviewPanel from "@/components/ReviewPanel"; import ProjectsPanel from "@/components/ProjectsPanel";
type Tab="briefing"|"projects"|"dump"|"review"; const TABS:[Tab,string][]=[["briefing","Briefing du jour"],["projects","Projets"],["dump","Décharge mentale"],["review","Bilan semaine"]];
const META={musique:{label:"Musique",ch:"CH.01",text:"text-amber",border:"border-amber"},esport:{label:"Esport",ch:"CH.02",text:"text-cyan",border:"border-cyan"},vie:{label:"Vie",ch:"CH.03",text:"text-teal",border:"border-teal"}};
export default function DomainConsole({domain}:{domain:Domain}){const meta=META[domain];const[state,setState]=useState<DomainState>(loadDomainState(domain));const[modeRouge,setModeRouge]=useState(false);const[hydrated,setHydrated]=useState(false);const[tab,setTab]=useState<Tab>("briefing");useEffect(()=>{setState(loadDomainState(domain));setModeRouge(loadModeRouge());setHydrated(true)},[domain]);useEffect(()=>{if(hydrated)saveDomainState(domain,state)},[state,hydrated,domain]);if(!hydrated)return null;const toggle=()=>{const n=!modeRouge;setModeRouge(n);saveModeRouge(n)};const toTask=(id:string,text:string)=>setState(s=>({...s,dump:s.dump.filter(i=>i.id!==id),briefing:{...s.briefing,tasks:[...s.briefing.tasks,{id:crypto.randomUUID(),text,done:false}]}}));return <main className="min-h-screen flex flex-col"><header className="border-b border-white/10 px-6 py-4 flex items-center justify-between gap-4"><div className="flex items-center gap-3"><Link href="/" className="font-mono text-[11px] uppercase tracking-widest text-muted border border-white/15 rounded-full px-3 py-1.5">← Accueil</Link><div className={`flex items-center gap-2 ${modeRouge?"text-alert":meta.text}`}><DomainIcon domain={domain} className="w-5 h-5"/><div><h1 className="font-display text-xl">{meta.label}</h1><p className="text-[10px] font-mono text-muted">{meta.ch}</p></div></div></div><ModeRougeSwitch active={modeRouge} onToggle={toggle}/></header>{modeRouge&&<div className="bg-alert/10 border-b border-alert/30 text-alert text-xs font-mono uppercase px-6 py-2">● Mode rouge — essentiel uniquement</div>}<nav className="flex gap-1 px-6 pt-4 overflow-x-auto">{TABS.map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={`px-4 py-2 border-b-2 whitespace-nowrap font-mono text-xs uppercase tracking-widest ${tab===id?(modeRouge?"border-alert text-ink":`${meta.border} text-ink`):"border-transparent text-muted"}`}>{label}</button>)}</nav><section className="flex-1 px-6 py-8 max-w-3xl w-full mx-auto">{tab==="briefing"&&<BriefingPanel data={state.briefing} modeRouge={modeRouge} onChange={(b:BriefingData)=>setState(s=>({...s,briefing:b,completedThisWeek:syncWeeklyCompletedTasks(s.completedThisWeek,b)}))}/>} {tab==="projects"&&<ProjectsPanel projects={state.projects} onChange={projects=>setState(s=>({...s,projects}))}/>} {tab==="dump"&&<DumpPanel items={state.dump} onChange={items=>setState(s=>({...s,dump:items}))} onCreateTask={toTask}/>} {tab==="review"&&<ReviewPanel draft={state.reviewDraft} history={state.reviewHistory} completedTasks={state.completedThisWeek} onDraftChange={d=>setState(s=>({...s,reviewDraft:d}))} onSave={entry=>setState(s=>({...s,reviewHistory:[entry,...s.reviewHistory],reviewDraft:emptyReviewDraft(),completedThisWeek:[]}))}/>}</section></main>}
