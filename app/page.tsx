"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {Domain,DomainState,loadDomainState,loadModeRouge,saveModeRouge,workloadScore,localDateKey} from "@/lib/storage";import DomainIcon from "@/components/DomainIcon";import ModeRougeSwitch from "@/components/ModeRougeSwitch";
const CARDS=[{domain:"musique" as Domain,href:"/musique",ch:"CH.01",label:"Musique",text:"text-amber",border:"hover:border-amber/60",dot:"bg-amber"},{domain:"esport" as Domain,href:"/esport",ch:"CH.02",label:"Esport",text:"text-cyan",border:"hover:border-cyan/60",dot:"bg-cyan"},{domain:"vie" as Domain,href:"/vie",ch:"CH.03",label:"Vie",text:"text-teal",border:"hover:border-teal/60",dot:"bg-teal"}];
export default function Home(){const[modeRouge,setModeRouge]=useState(false);const[states,setStates]=useState<Record<Domain,DomainState>|null>(null);useEffect(()=>{setModeRouge(loadModeRouge());setStates({musique:loadDomainState("musique"),esport:loadDomainState("esport"),vie:loadDomainState("vie")})},[]);const toggle=()=>{const n=!modeRouge;setModeRouge(n);saveModeRouge(n)};if(!states)return null;const totalDone=CARDS.reduce((n,c)=>n+states[c.domain].completedThisWeek.length,0);const urgent=CARDS.reduce((n,c)=>n+states[c.domain].dump.filter(i=>i.category==="URGENT").length,0);const scores=CARDS.map(c=>workloadScore(states[c.domain]));const globalLoad=Math.round(scores.reduce((a,b)=>a+b,0)/scores.length);const today=localDateKey();const deadlines=CARDS.flatMap(c=>states[c.domain].projects.filter(p=>!p.done&&p.dueDate).map(p=>({...p,domain:c.label,href:c.href}))).filter(p=>p.dueDate!>=today).sort((a,b)=>a.dueDate!.localeCompare(b.dueDate!)).slice(0,4);return <main className="min-h-screen flex flex-col"><header className="border-b border-white/10 px-6 py-5 flex flex-wrap items-center justify-between gap-4"><div><h1 className="font-display text-3xl">IBES</h1><p className="text-xs text-muted font-mono uppercase tracking-widest">console personnelle</p></div><ModeRougeSwitch active={modeRouge} onToggle={toggle}/></header>{modeRouge&&<div className="bg-alert/10 border-b border-alert/30 text-alert text-xs font-mono uppercase tracking-widest px-6 py-2">● Mode rouge actif — protège l'essentiel</div>}<section className="flex-1 px-6 py-8 max-w-5xl w-full mx-auto space-y-8"><div><p className="font-mono text-[11px] text-muted uppercase tracking-widest mb-3">Cockpit</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-3"><div className="bg-panel border border-white/10 rounded-lg p-4"><div className={`font-display text-2xl ${globalLoad>=75?"text-alert":globalLoad>=50?"text-amber":"text-teal"}`}>{globalLoad}%</div><div className="text-[10px] font-mono uppercase tracking-widest text-muted">charge actuelle</div></div><div className="bg-panel border border-white/10 rounded-lg p-4"><div className="font-display text-2xl">{totalDone}</div><div className="text-[10px] font-mono uppercase tracking-widest text-muted">réalisations semaine</div></div><div className="bg-panel border border-white/10 rounded-lg p-4"><div className={urgent?"font-display text-2xl text-alert":"font-display text-2xl"}>{urgent}</div><div className="text-[10px] font-mono uppercase tracking-widest text-muted">urgences</div></div><div className="bg-panel border border-white/10 rounded-lg p-4"><div className={`font-display text-lg ${modeRouge?"text-alert":"text-teal"}`}>{modeRouge?"ROUGE":"NORMAL"}</div><div className="text-[10px] font-mono uppercase tracking-widest text-muted">état système</div></div></div>{globalLoad>=75&&!modeRouge&&<div className="mt-3 rounded-md border border-alert/30 bg-alert/10 px-4 py-3 text-sm"><span className="text-alert font-medium">Charge élevée.</span><span className="text-muted"> Le mode rouge peut être pertinent aujourd'hui.</span></div>}</div><div><p className="font-mono text-[11px] text-muted uppercase tracking-widest mb-3">Aujourd'hui</p><div className="grid sm:grid-cols-3 gap-5">{CARDS.map((c,idx)=>{const b=states[c.domain].briefing;const priorities=[[b.p1,b.p1Done],[b.p2,b.p2Done],[b.p3,b.p3Done]] as const;const open=b.tasks.filter(t=>!t.done).length;return <Link key={c.domain} href={c.href} className={`rounded-xl border border-white/10 bg-panel px-5 py-5 ${c.border}`}><div className="flex items-center justify-between mb-4"><div className={`flex items-center gap-2 ${modeRouge?"text-alert":c.text}`}><DomainIcon domain={c.domain} className="w-5 h-5"/><span className="font-display">{c.label}</span></div><span className="font-mono text-[10px] text-muted">{scores[idx]}%</span></div><div className="space-y-2 min-h-[5.5rem]">{priorities.map(([text,done],i)=>text?<div key={i} className={`text-sm flex gap-2 ${done?"text-muted line-through":""}`}><span className="font-mono text-[10px] text-muted pt-1">0{i+1}</span><span>{text}</span></div>:null)}</div><div className="mt-4 pt-3 border-t border-white/10 flex justify-between text-[10px] font-mono uppercase tracking-widest text-muted"><span>{open} tâche{open!==1?"s":""}</span><span>Ouvrir →</span></div></Link>})}</div></div>{deadlines.length>0&&<div><p className="font-mono text-[11px] text-muted uppercase tracking-widest mb-3">Échéances projets</p><div className="space-y-2">{deadlines.map(p=><Link key={p.id} href={p.href} className="flex items-center justify-between bg-panel border border-white/10 rounded-md px-4 py-3"><div><span className="text-sm">{p.name}</span><span className="text-[10px] font-mono uppercase text-muted ml-2">{p.domain}</span></div><span className="text-xs font-mono text-amber">{new Date(p.dueDate!+"T00:00:00").toLocaleDateString("fr-FR")}</span></Link>)}</div></div>}</section><footer className="px-6 py-4 text-center font-mono text-[10px] text-muted/70 uppercase tracking-widest">Données stockées uniquement dans ce navigateur</footer></main>}
